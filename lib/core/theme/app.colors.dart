import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFF0D0D0D); // Fundo profundo
  static const surface = Color(0xFF171717);    // Cards e inputs
  static const cardBorder = Color(0xFF262626); // Bordas sutis
  
  static const primary = Color(0xFF6C63FF);    // Roxo Questify
  static const accent = Color(0xFF00F5D4);     // Verde Neon (Sucesso/XP)
  static const fire = Color(0xFFFF5D22);       // Laranja (Streak)
  static const shield = Color(0xFF3B82F6);     // Azul (Escudo)
  
  static const textPrimary = Color(0xFFEDEDED);
  static const textSecondary = Color(0xFFA1A1A1);
}